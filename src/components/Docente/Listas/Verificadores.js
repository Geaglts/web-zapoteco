import React from "react";
import { View, Text } from "react-native";

const Verificadores = () => {
    return (
        <View>
            <Text></Text>
        </View>
    );
};

export default Verificadores;
